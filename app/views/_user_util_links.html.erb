<div class="navbar-right">
  <% if current_user.is? :admin or current_user.is? :institutional_admin %>
    <ul class="nav navbar-nav">
      <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown">Admin</strong> <b class="caret"></b></a>
        <ul class="dropdown-menu">
          <li><%= link_to t('blacklight.header_links.manage_users'), users_path %></li>
          <li><%= link_to t('blacklight.header_links.new_user'), new_user_path %></li>
          <% if can? :read, Institution %>
            <li class="divider"></li>
            <li><%= link_to t('blacklight.header_links.manage_insitutions'), institutions_path %></li>
          <% end %>
          <% if can? :create, Institution %>
            <li><%= link_to t('blacklight.header_links.new_institution'), new_institution_path %></li>
          <% end %>
        </ul>
      </li>
    </ul>
  <% end %>

  <ul class="nav navbar-nav">
    <li>
        <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= current_user.to_s %></strong> <b class="caret"></b></a>
        <ul class="dropdown-menu">
          <li><%= link_to t('blacklight.header_links.bookmarks'), bookmarks_path %></li>
          <li><%= link_to t('blacklight.header_links.saved_searches'), saved_searches_path %></li>
          <li><%= link_to t('blacklight.header_links.search_history'), search_history_path %></li>
          <li class="divider"></li>
          <li><%= link_to "Edit Profile", edit_user_path(current_user) %></li>
          <li><%= link_to "Change Password", edit_password_user_path(current_user), method: :get %></li>
          <li class="divider"></li>
          <li><%= link_to t('blacklight.header_links.logout'), destroy_user_session_path, method: :delete %></li>
        </ul>
    </li>
  </ul>
</div>
